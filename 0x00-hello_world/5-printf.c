#include <stdio.h>

/**
 * main - prints the phrase with proper grammar,
 * but the outcome is a piece of art.
 * Retur: 0 if exited properly, non-zero otherwise.
 */
int main(void)
{
	printf("with propper grammar, but the outcome is a piece of art,\n");
	return (0);

}
